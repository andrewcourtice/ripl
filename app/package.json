{
    "private": true,
    "scripts": {
        "generate-api": "node scripts/generate-api.mjs && node scripts/generate-api-pages.mjs",
        "prepare-playground": "node scripts/prepare-playground.mjs",
        "start": "yarn generate-api && yarn prepare-playground && vitepress dev ./src",
        "build": "yarn generate-api && yarn prepare-playground && vitepress build ./src",
        "preview": "vitepress preview ./src"
    },
    "dependencies": {
        "@ripl/3d": "1.0.0-beta.0",
        "@ripl/charts": "1.0.0-beta.0",
        "@ripl/core": "1.0.0-beta.0",
        "@ripl/svg": "1.0.0-beta.0",
        "@ripl/utilities": "1.0.0-beta.0",
        "flex-layout-attribute": "^1.0.3",
        "lucide-vue-next": "^0.577.0",
        "monaco-editor": "^0.55.1",
        "pinia": "^3.0.4",
        "sass": "^1.97.3",
        "vue": "^3.5.30"
    },
    "devDependencies": {
        "vitepress": "^1.6.4",
        "vitepress-plugin-tabs": "^0.8.0"
    }
}
