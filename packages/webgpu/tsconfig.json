// Build-only: `lib.dom` declares the WebGPU interfaces but not the `GPU*Usage` flag objects used as values.
{
    "extends": "../../tsconfig.json",
    "compilerOptions": {
        "types": [
            "@webgpu/types"
        ]
    },
    "include": [
        "src/**/*.ts",
    ]
}
