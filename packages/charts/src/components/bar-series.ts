import {
    ChartComponent,
} from './_base';

/** A reusable bar series component for rendering grouped or stacked bar segments. */
export class ChartBarSeries extends ChartComponent {

    /** Placeholder render pass: bar segments are currently drawn by the owning chart, so this is a no-op. */
    public async render() {
        // Placeholder: the owning chart draws the bar segments.
    }

}